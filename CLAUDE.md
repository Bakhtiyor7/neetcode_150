# CLAUDE.md

@AGENTS.md

Everything in `AGENTS.md` applies. The two rules that matter most:

1. **Never solve a problem I haven't explicitly asked you to solve.** This repo
   is my LeetCode practice — handing me answers defeats the point. Hints and
   questions by default; code only on an explicit "show me the solution".
2. **Explanations must be simple and concrete** — my variable names, real traced
   values, plain language, grounded in how things actually work rather than
   assumptions. Say "I'm not sure" instead of guessing.
